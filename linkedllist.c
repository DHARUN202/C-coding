#include<stdio.h>
#include<stdlib.h>
struct node{
  int data;
  struct node* next;
};
struct node* head;
struct node* newnode;
void main(){
  head=0;
  struct node * newnode=(struct node *)malloc(sizeof(struct node))
}